# Session State

Use this file as the handoff point between Codex sessions.

At the start of a new chat, ask:
- `Read AGENTS.md and docs/session_state.md, then continue from the saved state.`

At the end of a task, ask:
- `Update docs/session_state.md with the current status before we stop.`

## Current Task
- Goal: Monitor and continue relay-aware DQN experiments for `complex_charge_bastion`.
- Current status: Relay-aware DQN solves `complex_charge_bastion` for seeds `416`, `417`, and `418`. PPO has been added as the next learning-family baseline and solves `default`. On 2026-04-30, guided exploration was fixed so the shared encoder's guided policy solves `charge_maze_medium` in 34 steps and `complex_charge_bastion` in 150 steps under the evaluation battery profile. PPO with curriculum, guided warm-start, and mixed guided imitation loss solves `charge_maze_medium` after 5000 episodes with `avg_reward=37.50`, `avg_steps=42`, `avg_cleaned_ratio=100%`, and `success_rate=100%`, reproduced over 50 and 200 CPU evaluation episodes. PPO with final-relay curriculum, staged DAgger, and periodic BC now solves `complex_charge_bastion`: best checkpoint at episode `4500` reproduced `avg_reward=-53.00`, `avg_steps=150`, `avg_cleaned_ratio=100%`, and `success_rate=100%` over 20, 50, and 200 CPU eval episodes. The pygame UI now supports script-backed `dqn`, `ppo`, and `ppo_guided` training, plus `dqn_best` and `ppo_best` playback of existing best-eval checkpoints. UI DQN seed `421` with 7000 episodes failed (`best_eval=1/3`, final `0/3`), so it is a negative run rather than a new reference.
- Priority: Treat `outputs/checkpoints/ppo_agent_complex_charge_bastion_best_eval_seed_42_ppo_finalrelay_curriculum6500.pt` as the current PPO `bastion` reference checkpoint. The final checkpoint from the same run regressed to `2/3` after the trailing rollout update, so comparison should use the saved best-eval checkpoint. Use `ppo_guided` in the UI for the staged guided-imitation recipe on hard charge maps. Add SARSA later for the final Q-learning vs DQN vs PPO vs SARSA comparison.

## Working Context
- Relevant files: `scripts/train_dqn.py`, `utils/dqn_experiment_utils.py`, `agents/dqn_agent.py`, `outputs/checkpoints/dqn_agent_complex_charge_bastion_best_eval_seed_417_v2best.pt`, `outputs/checkpoints/dqn_agent_complex_charge_bastion_ep_10000_seed_417_v2best.pt`, `outputs/checkpoints/dqn_agent_complex_charge_bastion_best_eval_seed_417_v2relayprobe.pt`, `outputs/checkpoints/dqn_agent_complex_charge_bastion_ep_2000_seed_417_v2relayprobe.pt`, `outputs/logs/dqn_best_eval_complex_charge_bastion_ep_10000_seed_417_v2best.out.log`, `outputs/logs/dqn_best_eval_complex_charge_bastion_ep_10000_seed_417_v2best.err.log`, `outputs/logs/dqn_best_eval_complex_charge_bastion_ep_10000_seed_417_v2best_resume1000.out.log`, `outputs/logs/dqn_best_eval_complex_charge_bastion_ep_10000_seed_417_v2best_resume1000.err.log`, `outputs/logs/dqn_best_eval_complex_charge_bastion_ep_10000_seed_417_v2best_resume1000_diag.out.log`, `outputs/logs/dqn_best_eval_complex_charge_bastion_ep_10000_seed_417_v2best_resume1000_diag.err.log`, `outputs/logs/codex_dqn_bastion_v2best_resume_1000_launch.ps1`, `outputs/plots/dqn_training_reward_complex_charge_bastion.png`, `outputs/plots/dqn_training_cleaned_ratio_complex_charge_bastion.png`, `outputs/plots/dqn_training_success_complex_charge_bastion.png`, `outputs/plots/dqn_training_epsilon_complex_charge_bastion.png`, `outputs/plots/dqn_training_loss_complex_charge_bastion.png`
- Commands already run: Checked scheduled-task status, tailed original logs, confirmed best checkpoint creation, inspected resume semantics in `train_dqn.py`, created a detached launcher, relaunched training from the best-eval checkpoint with `--starting-checkpoint-episodes 1000`, and on 2026-04-27 launched a fresh diagnostic resume when no active process remained.
- Validation completed: Confirmed the resumed run wrote the expected header including `resume_best_eval: checkpoint_episodes=1000 | avg_reward=-231.00 | avg_steps=117.00 | avg_cleaned_ratio=33.33% | success_rate=0.00%`. Confirmed on 2026-04-27 that the old 2026-04-24 resume was not running, the best-eval checkpoint timestamp had not advanced, and a new active Python training process existed for the same resume command. Waited through completion of the 2026-04-27 resume and confirmed the run finished cleanly with a final checkpoint, plots, and an updated best-eval checkpoint. Re-evaluated both the saved `best_eval` and final `ep_10000` checkpoints on CPU with fixed RNG seed and reproduced the saved metrics exactly. Traced both greedy rollouts step by step, including recharge events and dirty-tile clean events. Updated `agents/dqn_agent.py` so `guided_action()` now prefers a relay charger that unlocks reachable remaining dirty tiles instead of blindly minimizing distance to the nearest charger when no dirty tile is currently safe. Ran a focused 1000-episode relay probe (`checkpoint_tag=v2relayprobe`) from the old 1000-episode `v2best` best checkpoint and observed `eval@2000: avg_reward=-180.00 | avg_steps=222.00 | avg_cleaned_ratio=100.00% | success_rate=100.00%`, then reproduced that result on CPU for both 50 and 200 evaluation episodes. Fixed `scripts/train_dqn.py` so resume runs apply CLI training overrides such as `--epsilon-decay`, `--epsilon-min`, `--learning-rate`, and guided exploration ratio to the loaded agent config and optimizer. Added relay-progress reward shaping flags and final-dirty/multi-charger relay guidance, then verified seed `418` tag `v2relay_shape_finalroute` reaches `avg_reward=-83.50`, `avg_steps=168`, `avg_cleaned_ratio=100%`, `success_rate=100%`; reproduced the best checkpoint over both 50 and 200 CPU evaluation episodes. Later DQN performance checks found shortest feasible route is `150` steps. Continuing seed `418` from `3000` to `4000` with the same high epsilon did not improve best eval. Fine-tuning from the best checkpoint with `--resume-epsilon 0.2`, `learning_rate=0.0005`, and tag `v2relay_finetune_eps02` improved training success but worsened greedy eval to `174` steps. Removing route safety reserve from route feasibility was tested under tag `v2route_noreserve` and failed, so that code change was reverted. Added PPO and verified `.venv\Scripts\python.exe -m pytest tests` plus a smoke run: `.venv\Scripts\python.exe scripts/train_ppo.py --map-name default --battery-profile evaluation --episodes 5 --seed 42 --print-every 5 --rollout-steps 32 --update-epochs 1 --minibatch-size 16 --hidden-size 32 --eval-episodes 2 --eval-every 0 --checkpoint-tag smoke --device cpu`. Added PPO curriculum stages and guided behavior-cloning warm-start support, verified the new tests with `.venv\Scripts\python.exe -m pytest tests`, ran a combined smoke test, and ran a 5000-episode combined curriculum/guided experiment on `charge_maze_medium`. On 2026-04-30, added mixed guided imitation loss to PPO updates, fixed guided action priority/relaxed recovery so the guided policy solves `charge_maze_medium` and improves `complex_charge_bastion` to the shortest known 150-step route, then verified `.venv\Scripts\python.exe -m pytest tests` with 31 passing tests. Ran PPO `charge_maze_medium` 5000 episodes with tag `ppo_guidancefix_imitation5000` and reproduced the best checkpoint over 50 and 200 CPU eval episodes. After UI training integration, `.venv\Scripts\python.exe -m pytest tests` passes with 38 tests. After the UI progress fix and docs update, `.venv\Scripts\python.exe -m pytest tests` passes with 39 tests.
- Validation not run: No pygame UI playback check yet for the completed `v2best` checkpoints. No relay-aware continuation beyond cumulative episode `5000` has been run for seed `418`.

## Decisions
- Resume from `dqn_agent_complex_charge_bastion_best_eval_seed_417_v2best.pt` instead of restarting from scratch.
- Use a detached PowerShell launcher under `outputs/logs/` because the earlier interactive scheduled task was being terminated by a window-close event.
- Pass `--starting-checkpoint-episodes 1000` explicitly because the best-eval checkpoint filename does not encode episode count.
- Keep the resulting best-eval checkpoint chosen by the existing comparison logic even though its average reward is worse than earlier evaluations, because the current ranking prioritizes success rate and cleaned ratio before reward.

## Open Questions
- Does the current best-eval ranking rule reflect the intended objective for `complex_charge_bastion`, given that successful checkpoints can appear transiently before later checkpoints regress?
- Should feature-version metadata be bumped after the final-dirty and multi-charger relay semantic change, or is it acceptable as a compatible improvement because feature vector size and checkpoint loading still work?

## Next Steps
1. Preserve `charge_maze_medium` PPO success as the current medium policy-gradient reference checkpoint: `outputs/checkpoints/ppo_agent_charge_maze_medium_best_eval_seed_42_ppo_guidancefix_imitation5000.pt`.
2. For another DQN `bastion` attempt, increase guided exploration exposure and keep exploration alive longer than the failed seed `421` UI run.
3. Add SARSA after PPO/UI documentation is updated, then build a table across Q-learning, DQN, PPO, and SARSA.

## Notes For Future Sessions
- Preserve checkpoint naming compatibility.
- Keep CLI entry points thin and reusable logic outside `scripts/`.
- Treat `outputs/` as generated artifacts unless the task is specifically about them.

## Last Updated
- 2026-04-24: Created persistent session handoff template.
- 2026-04-24: Saved the `v2best` resume state after detached relaunch from the 1000-episode best-eval checkpoint.
- 2026-04-27: Verified the 2026-04-24 resume was no longer active, then launched a fresh resume from the same 1000-episode best-eval checkpoint and confirmed the process is running under `resume1000_diag` logs.
- 2026-04-27: Observed the fresh `resume1000_diag` run complete through cumulative episode 10000. Best-eval updated at cumulative episodes 1500 and 6000, with the final selected best checkpoint saved at 6000 and the final full checkpoint saved at 10000.
- 2026-04-27: Compared `best_eval` against `ep_10000` directly. `best_eval` reproduces `avg_reward=-1125.50`, `avg_steps=588`, `avg_cleaned_ratio=66.67%`, `success_rate=0%`; `ep_10000` reproduces `avg_reward=-913.50`, `avg_steps=588`, `avg_cleaned_ratio=33.33%`, `success_rate=0%`. The two policies first diverge at step 42; `best_eval` cleans a second tile at step 98 while `ep_10000` never does.
- 2026-04-27: Traced recharge behavior and found both policies do recharge repeatedly. `best_eval` recharges 10 times at the upper charger and cleans tiles at steps 39 and 98 before looping; `ep_10000` recharges 11 times at the same upper charger and cleans only the first tile. A state-search sanity check confirmed the map is solvable at battery 65 via `R -> D(top-right) -> C(upper) -> D(bottom-right) -> C(upper) -> C(lower) -> D(left)`. This suggests the DQN representation/guidance is missing the charger-to-charger relay pattern needed for the last dirty tile.
- 2026-04-27: Implemented relay-aware guided exploration in `agents/dqn_agent.py`. In states where no dirty tile is directly safe, `guided_action()` now picks a reachable charger that maximizes future safe-dirty reachability after recharge rather than drifting toward the nearest charger. Verified with a focused check that state `(row=5, col=15, cleaned_mask=5, battery=65)` selects the lower charger `(15, 10)` as the relay target and chooses a move that starts that transfer.
- 2026-04-27: Verified the relay-aware change empirically with a new probe run tagged `v2relayprobe`, resumed from the old 1000-episode `v2best` best checkpoint for 1000 more episodes. The resulting `dqn_agent_complex_charge_bastion_best_eval_seed_417_v2relayprobe.pt` solves the map deterministically in evaluation: `avg_reward=-180.00`, `avg_steps=222`, `avg_cleaned_ratio=100%`, `success_rate=100%`. A traced greedy rollout cleaned tiles at steps 39, 100, and 222, with recharge sequence at upper charger, upper charger, upper charger, then lower charger before the final left-side dirty tile.
- 2026-04-28: Generated `outputs/gifs/dqn_bastion_v2best_vs_v2relayprobe.gif`, a side-by-side visual comparison of old seed `417` `v2best` best-eval against new seed `417` `v2relayprobe`. The old policy ends with `2/3` cleaned by `step_limit`; the relay-aware policy reaches `3/3` cleaned and `all_cleaned` at 222 steps.
- 2026-04-28: Ran short from-scratch relay-aware seed checks tagged `v2relayseedcheck` for seeds `416` and `418`, each for 2000 episodes with feature version 2 and guided exploration ratio `0.6`. Both runs produced training-time success episodes but no greedy evaluation success. Seed `416` best-eval remained `1/3` cleaned at checkpoint episode 1000 (`avg_reward=-182.50`, `avg_steps=87`, `success_rate=0%`). Seed `418` best-eval remained `1/3` cleaned at checkpoint episode 1500 (`avg_reward=-177.50`, `avg_steps=87`, `success_rate=0%`), while the final eval at 2000 regressed to a `588`-step `1/3` loop. Existing older seed `416/418` 5000 checkpoints have no `feature_version` metadata and are not directly compatible with the current feature-version 2 resume path.
- 2026-04-28: Continued seed `416` from cumulative episode `2000` to `5000` under the same `v2relayseedcheck` tag. Greedy eval improved to `2/3` at `2500` and `3000`, regressed to `1/3` at `3500` and `4000`, then reached full success at `4500`: `avg_reward=-158.00`, `avg_steps=202`, `avg_cleaned_ratio=100%`, `success_rate=100%`. The final `5000` checkpoint regressed to `1/3`, but the best-eval checkpoint at `4500` was saved and reproduced `100%` success over both 50 and 200 CPU reevaluation episodes.
- 2026-04-28: Continued seed `418` from cumulative episode `2000` to `5000` under the same `v2relayseedcheck` tag. Training success rose as high as roughly `29%`, but greedy evaluation never exceeded `1/3`; `eval@4000` briefly collapsed to `0/3`, and final `eval@5000` was also `0/3`. The best-eval checkpoint remains the earlier `1500` checkpoint with `1/3` cleaned and `0%` success.
- 2026-04-28: Discovered that `scripts/train_dqn.py` did not apply CLI hyperparameter overrides after loading `--init-checkpoint`; the first attempted `v2relay_epsslow` seed `418` run was stopped because it still used the checkpoint's old `epsilon_decay=0.99995` and `epsilon_min=0.10`. Updated the resume path so loaded agents receive CLI overrides for learning rate, discount factor, epsilon decay/floor, batch size, learning starts, train cadence, target update interval, and guided exploration ratio; optimizer param groups now receive the CLI learning rate too. Hidden size is now validated on resume.
- 2026-04-28: Re-ran seed `418` from cumulative episode `2000` to `5000` with corrected slower epsilon settings under tag `v2relay_epsslow`: `epsilon_decay=0.99997`, `epsilon_min=0.15`, `eval_every=250`. This did not improve greedy success. Best-eval was only `1/3` at checkpoint episode `3500` (`avg_reward=-157.50`, `avg_steps=87`, `success_rate=0%`); later evaluations repeatedly collapsed to `0/3`, and final `eval@5000` was also `0/3`.
- 2026-04-28: Traced seed `418` failures and found two issues: greedy Q-values ignored relay guidance after the first dirty tile, and after reaching `2/3`, the guidance rejected the required `upper charger -> lower charger -> final dirty` chain because it required charger recovery after the final dirty tile. Added relay-progress reward shaping flags to `GridCleanEnv`/`train_dqn.py`, allowed final dirty routes without recovery cost, and extended relay charger selection to consider one charger-to-charger hop. A new seed `418` resume tagged `v2relay_shape_finalroute` from cumulative episode `2000` to `3000` reached deterministic success: best/final eval at `3000` was `avg_reward=-83.50`, `avg_steps=168`, `avg_cleaned_ratio=100%`, `success_rate=100%`, reproduced over both 50 and 200 CPU evaluation episodes.
- 2026-04-28: Checked DQN efficiency after success. Shortest feasible path for `complex_charge_bastion` at battery `65` is `150` steps with reward around `-50.5`; current best seed `418` policy remains `168` steps and `avg_reward=-83.50`. Continuing with high epsilon to cumulative `4000` did not improve best eval. Fine-tuning with `--resume-epsilon 0.2` improved training-time success (`last_100_success_rate=84%`) but worsened greedy eval to `174` steps. Testing no-reserve route feasibility failed (`v2route_noreserve` collapsed by final eval), so reserve logic was restored. Added a `--resume-epsilon` option and baseline best-eval registration for safer future fine-tuning runs.
- 2026-04-28: Added PPO as the next learning-family baseline. New files: `agents/ppo_agent.py`, `utils/ppo_experiment_utils.py`, `scripts/train_ppo.py`, and `tests/test_ppo_agent.py`. PPO uses the existing DQN `StateFeatureEncoder` and action masks, an actor-critic MLP, GAE, clipped objective, entropy bonus, checkpoint save/load, best-eval checkpoint helpers, and plotting. Smoke run on `default` for 5 episodes completed end-to-end, and tests passed with `.venv\Scripts\python.exe -m pytest tests`.
- 2026-04-28: Committed PPO baseline as `a412059 feat: add ppo training baseline`. Ran PPO `default` 3000-episode baseline with tag `ppo3000`: best eval at episode `1000` was `avg_reward=95.00`, `avg_steps=13`, `avg_cleaned_ratio=100%`, `success_rate=100%`; final eval at episode `3000` was `avg_reward=91.00`, `avg_steps=15`, `success_rate=100%`. Best checkpoint: `outputs/checkpoints/ppo_agent_default_best_eval_seed_42_ppo3000.pt`.
- 2026-04-28: Ran PPO `complex_charge_bastion` smoke test for 10 episodes with tag `bastion_smoke`; training completed end-to-end but eval remained `0/3` cleaned. Then ran PPO `complex_charge_bastion` 5000-episode baseline with tag `ppo5000`, seed `42`, evaluation battery `65`, `rollout_steps=2048`, `update_epochs=4`, `minibatch_size=256`, `hidden_size=256`. Greedy eval never cleaned any dirty tile: best checkpoint at episode `2500` had `avg_reward=-156.50`, `avg_steps=65`, `avg_cleaned_ratio=0%`, `success_rate=0%`; final eval at episode `5000` matched `avg_reward=-156.50`, `avg_steps=65`, `0%` cleaned. This indicates plain PPO needs curriculum/guided exploration/reward support before the bastion map.
- 2026-04-28: Ran PPO intermediate charge-map test on `charge_maze_medium` for 5000 episodes with tag `ppo5000`, seed `42`, evaluation battery `29`, `rollout_steps=2048`, `update_epochs=4`, `minibatch_size=256`, `hidden_size=256`. PPO improved from `0/3` to stable `1/3` greedy cleaned ratio but did not reach success. Best checkpoint at episode `3500`: `avg_reward=-81.00`, `avg_steps=29`, `avg_cleaned_ratio=33.33%`, `success_rate=0%`; final eval at episode `5000` was the same. This suggests PPO exploration can find the first dirty tile on medium charge maps but still needs curriculum/guidance for multi-dirty charger planning.
- 2026-04-28: Implemented both PPO curriculum and guided warm-start. `scripts/train_ppo.py` now supports staged precleaned training with `--curriculum-stage-keep-dirty-indices` and `--curriculum-stage-episodes`, plus guided behavior-cloning warm-start via `--guided-warm-start-episodes`, `--guided-warm-start-epochs`, `--guided-warm-start-minibatch-size`, and `--guided-warm-start-per-stage`. `agents/ppo_agent.py` now exposes `behavior_clone_update(...)`, and parser/curriculum helper tests were added in `tests/test_train_ppo.py`.
- 2026-04-28: Ran PPO curriculum plus guided warm-start on `charge_maze_medium` for 5000 episodes with tag `ppo_curriculum_guided5000`, seed `42`, stages `2 -> 1,2 -> full`, stage episodes `1500/1500/2000`, guided warm-start `20` episodes per stage, `rollout_steps=2048`, `update_epochs=4`, `minibatch_size=256`, `hidden_size=256`. Full-map greedy eval reached `1/3` at episodes `2500` and `3000`, but regressed to `0/3` in the final full-map stage. Best checkpoint at episode `2500`: `avg_reward=-83.00`, `avg_steps=29`, `avg_cleaned_ratio=33.33%`, `success_rate=0%`; final eval at episode `5000`: `avg_reward=-99.00`, `avg_steps=29`, `avg_cleaned_ratio=0%`, `success_rate=0%`. This did not improve over plain PPO on the medium map.
- 2026-04-30: Added mixed guided imitation loss to PPO updates via `--guided-imitation-coef` and `--guided-imitation-episodes`. Also fixed shared guided exploration in `StateFeatureEncoder`: safe/relaxed dirty targets now take priority over emergency charger fallback, and a relaxed no-reserve recovery check prevents `charge_maze_medium` from looping at the charger. The standalone guided policy now solves `charge_maze_medium` in 34 steps and `complex_charge_bastion` in 150 steps under evaluation battery.
- 2026-04-30: Ran PPO `charge_maze_medium` with curriculum + guided warm-start + mixed imitation for 5000 episodes, tag `ppo_guidancefix_imitation5000`, seed `42`. Final/best eval reached `avg_reward=37.50`, `avg_steps=42`, `avg_cleaned_ratio=100%`, `success_rate=100%`; the best checkpoint reproduced exactly over both 50 and 200 CPU evaluation episodes. This is the first PPO success on an intermediate charge map.
- 2026-04-30: Ran PPO `complex_charge_bastion` with full-map guided warm-start + mixed imitation for 5000 episodes, tag `ppo_guidancefix_imitation5000`, seed `42`. It still cleaned `0/3`; best eval was only `avg_reward=-156.50`, `avg_steps=65`, `avg_cleaned_ratio=0%`, `success_rate=0%`. Additional pure BC checks with up to 100 guided episodes and 30 epochs also stayed at `0/3`, suggesting `bastion` PPO needs on-policy relabeling/DAgger rather than stronger one-shot cloning.
- 2026-04-30: Implemented DAgger-style PPO relabeling in `scripts/train_ppo.py` with `--guided-dagger-coef`, `--guided-dagger-buffer-capacity`, `--guided-dagger-refresh-every`, and `--guided-dagger-episodes-per-update`. Ran a smoke test and a 5000-episode `complex_charge_bastion` run tagged `ppo_dagger5000` using `guided_dagger_coef=0.5`, `guided_dagger_buffer_capacity=50000`, and labels from every training episode. Training cleaned ratio improved to roughly `33%`, but greedy eval stayed at `0/3`; best eval was `avg_reward=-163.00`, `avg_steps=65`, `avg_cleaned_ratio=0%`, `success_rate=0%`. Passive DAgger relabel mixing alone is not enough for `bastion`.
- 2026-04-30: Added periodic BC updates on the DAgger buffer with `--guided-dagger-bc-every`, `--guided-dagger-bc-epochs`, and `--guided-dagger-bc-minibatch-size`, then combined that with staged `bastion` curriculum `2 -> 1,2 -> full` for 5000 episodes under tag `ppo_dagger_bc_curriculum5000`. This produced the first PPO `bastion` greedy improvement beyond `0/3`: best checkpoint at episode `5000` reproduced `avg_reward=-270.00`, `avg_steps=143`, `avg_cleaned_ratio=66.67%`, `success_rate=0%` over 20, 50, and 200 CPU eval episodes. The final checkpoint regressed to `0/3` after the trailing rollout update, so use the saved best-eval checkpoint for comparison: `outputs/checkpoints/ppo_agent_complex_charge_bastion_best_eval_seed_42_ppo_dagger_bc_curriculum5000.pt`.
- 2026-04-30: Completed the guided PPO `bastion` recipe with final-relay curriculum `2 -> 0,2 -> 1,2 -> full`, stage episodes `800/1200/2000/2500`, `guided_dagger_coef=0.75`, `guided_dagger_bc_every=100`, and `guided_dagger_bc_epochs=2`, tag `ppo_finalrelay_curriculum6500`. Greedy eval reached full success at episode `4500` and stayed successful at periodic eval through episode `6500`; the saved best-eval checkpoint reproduced `avg_reward=-53.00`, `avg_steps=150`, `avg_cleaned_ratio=100%`, `success_rate=100%` over 20, 50, and 200 CPU eval episodes. The final checkpoint from the same run regressed to `2/3` after the trailing rollout update, so use `outputs/checkpoints/ppo_agent_complex_charge_bastion_best_eval_seed_42_ppo_finalrelay_curriculum6500.pt` as the PPO reference.
- 2026-04-30: Added UI playback support for existing `.pt` best checkpoints via `dqn_best` and `ppo_best` algorithm choices. The UI loader prefers known reference checkpoints for `complex_charge_bastion`: DQN seed `418` tag `v2relay_shape_finalroute`, and PPO seed `42` tag `ppo_finalrelay_curriculum6500`. Verified both loaders can resolve and load their `complex_charge_bastion` checkpoints, and `.venv\Scripts\python.exe -m pytest tests` passed with 35 tests.
- 2026-04-30: Added UI training support for `dqn`, `ppo`, and `ppo_guided`. The UI now launches `scripts/train_dqn.py` and `scripts/train_ppo.py`, saves best-eval checkpoints with UI tags, parses flexible train logs, and loads the trained DQN/PPO agent for playback. `ppo_guided` uses the staged guided-imitation/DAgger/periodic-BC recipe, with stage episode counts allocated to match the selected total episode budget. Verified `.venv\Scripts\python.exe -m pytest tests` with 38 passing tests.
- 2026-04-30: Fixed UI deep-training progress reporting by running subprocesses with unbuffered output and lowering DQN/PPO UI `print-every`/`eval-every` to `250` episodes. Re-evaluated the UI DQN seed `421` run: best-eval at episode `4000` reached only `1/3` cleaned (`avg_reward=-132.00`, `avg_steps=87`, `success_rate=0%`), and final episode `7000` regressed to `0/3`; this run is a negative DQN bastion trial. Verified `.venv\Scripts\python.exe -m pytest tests` with 39 passing tests.
